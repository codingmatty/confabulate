const db = require('../../server/db');

module.exports.up = async () => {
  // const data = await db.Model.query();
};

module.exports.down = async () => {
  // const data = await db.Model.query();
};
